{
  "name": "wazup",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @wazup/shared build && concurrently -n api,web -c blue,green \"pnpm --filter @wazup/api dev\" \"pnpm --filter @wazup/web dev\"",
    "build": "pnpm --filter @wazup/shared build && pnpm --filter @wazup/api build && pnpm --filter @wazup/web build",
    "db:migrate": "pnpm --filter @wazup/api db:migrate",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
